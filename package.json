{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.8.4",
    "yarn": "^1.12.3"
  },
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "e2e": "lerna run e2e",
    "webdriver-update": "lerna run webdriver-update",
    "webdriver-start": "lerna run webdriver-start"
  }
}
